DATABASE_URL="postgresql://usuari:contrasenya@localhost:5432/avpc_federacio_db?schema=public"
# Genera una clau pròpia amb: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=""
DIRECT_URL="postgresql://usuari:contrasenya@localhost:5432/avpc_federacio_db?schema=public"
FRONTEND_URL="https://avpc-federacio-frontend.onrender.com"
PORT=4000

# Claus VAPID per a notificacions push web.
# Genera-les UNA VEGADA amb: npx web-push generate-vapid-keys
# i enganxa aquí el resultat. No les canviïs després, o els usuaris
# hauran de tornar a activar les notificacions.
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
